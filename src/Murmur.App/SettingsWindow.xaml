<Window x:Class="Murmur.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Murmur Settings"
        Width="460"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True">
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="FieldLabel">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,12,0" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,6" />
            <Setter Property="Padding" Value="6,3" />
        </Style>
    </Window.Resources>

    <StackPanel Margin="20">
        <TextBlock Text="Murmur Settings" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,4" />
        <TextBlock Text="Everything runs locally on your PC. Changes apply right away."
                   Foreground="Gray" Margin="0,0,0,16" TextWrapping="Wrap" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Hotkey" Style="{StaticResource FieldLabel}" />
            <ComboBox Grid.Row="0" Grid.Column="1" x:Name="HotkeyCombo" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Activation" Style="{StaticResource FieldLabel}" />
            <ComboBox Grid.Row="1" Grid.Column="1" x:Name="ModeCombo" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Microphone" Style="{StaticResource FieldLabel}" />
            <ComboBox Grid.Row="2" Grid.Column="1" x:Name="MicCombo" />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Model" Style="{StaticResource FieldLabel}" />
            <ComboBox Grid.Row="3" Grid.Column="1" x:Name="ModelCombo" />

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Language" Style="{StaticResource FieldLabel}" />
            <ComboBox Grid.Row="4" Grid.Column="1" x:Name="LanguageCombo" />
        </Grid>

        <CheckBox x:Name="AutoStartCheck" Content="Start Murmur when I sign in to Windows"
                  Margin="0,16,0,0" />

        <TextBlock x:Name="ModelHint" Foreground="#B06000" TextWrapping="Wrap"
                   Margin="0,12,0,0" Visibility="Collapsed" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="SaveButton" Content="Save" Width="90" Height="30" IsDefault="True"
                    Click="OnSave" Margin="0,0,8,0" />
            <Button x:Name="CancelButton" Content="Cancel" Width="90" Height="30" IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
